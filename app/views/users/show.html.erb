<div class="main">
  <div class="inner">
    <div class="user__wrapper">
      <h2 class="page-heading">
        <%= "#{@user_name}さんの情報"%> <%# ビューファイルのインスタンス変数としてセットされ、初めて使用できる%>
      </h2>
      <table class="table">
        <tbody>
          <tr>
            <th class="table__col1">名前</th>
            <td class="table__col2"><%= "#{@user_name}" %></td>
          </tr>
          <tr>
            <th class="table__col1">プロフィール</th>
            <td class="table__col2"><%= "#{@user.profile}" %></td>
          </tr>
          <tr>
            <th class="table__col1">所属</th>
            <td class="table__col2"><%= "#{@user.occupation}" %></td>
          </tr>
          <tr>
            <th class="table__col1">役職</th>
            <td class="table__col2"><%= "#{@user.position}" %></td>
          </tr>
        </tbody>
      </table>

        <h2 class="page-heading">
        <%= "#{@user_name}さんのプロトタイプ"%>
      </h2>
      <div class="user__card">
        <% if @user.prototypes.any? %>
          <%= render partial: "users/prototype_card", collection: @user.prototypes, as: :prototype %>
        <% else %>
          <p>まだプロトタイプがありません。</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
 <%# 部分テンプレートでそのユーザーが投稿したプロトタイプ投稿一覧を表示する %>
 <%# renderメソッドのcollectionオプションは、部分テンプレートに渡すコレクションを指定するために使用されます。%>